/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com','plus.unsplash.com','img.freepik.com','images.pexels.com']
    }
};

export default nextConfig;
